apiVersion: v1
kind: Service
metadata:
  name: openclaw-llm-external
  namespace: default
spec:
  type: NodePort
  selector:
    app: openclaw-llm
    inference.llmkube.dev/service: openclaw-llm
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30088
      protocol: TCP
